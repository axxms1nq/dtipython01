# Function (call function)
# 1. Built-in/Standard function (เช่น print(), input(), len(), type(), int(), float(), str() etc.)
# 2. User-defined function (ฟังก์ชันที่ผู้ใช้สร้างขึ้นเอง)
# 3. Third-party function (ฟังก์ชันที่มาจากไลบรารีของบุคคลที่สาม เช่น numpy, pandas, matplotlib etc.)

# ประเภทของ function 4 ประเภท
# 1. no parameter , no return (ฟังก์ชันที่ไม่มีพารามิเตอร์และไม่มีการคืนค่า)
# 2. have parameter , no return (ฟังก์ชันที่มีพารามิเตอร์แต่ไม่มีการคืนค่า)
# 3. no parameter , have return (ฟังก์ชันที่ไม่มีพารามิเตอร์แต่มีการคืนค่า)
# 4. have parameter , have return (ฟังก์ชันที่มีพารามิเตอร์และมีการคืนค่า)